import { SignatureArgumentArray } from './SignatureArgumentArray'
import { SignatureOptionArray } from './SignatureOptionArray'

/**
 * @type {[string, SignatureArgumentArray, SignatureOptionArray]}
 */
export type SignatureParserResult = [string, SignatureArgumentArray, SignatureOptionArray]
